# Upstream liseur-sync's docs/openapi.yaml on main, as last reviewed here.
# The sha256 is over the raw file bytes; .github/workflows/upstream-spec.yml
# compares it weekly and files an issue when it moves.
#
# When that issue arrives: re-read the changed endpoints against
# src/liseur_mcp/client.py, run the read-only checks against the real instance
# (./scripts/smoke.sh needs LISEUR_URL reachable, so it is a LAN-side step),
# then replace the hash below and close the issue.
#
# last reviewed: 2026-09-25 (upstream f146e36)
38903c577aaadad8cc971d8fd8d90e8bfe70dff54ae8abbc530cc51de0eea76f
